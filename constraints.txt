# constraints.txt — the tested backend set (ADR-0031: reproducibility is version-relative).
# Reproduce a known-good run exactly:  pip install -e . -c constraints.txt
# (or add [solvers] for the optional CaDiCaL backend). Update when the tested set moves.

z3-solver==5.0.0.0
sympy==1.14.0
mpmath==1.3.0
mcp==1.27.0
python-sat==1.9.dev7
ruff==0.15.11

# Repository-owned bootstrap and test toolchain (MH-C-ENV-001).
pip==26.2.1
pytest==8.4.2
pytest-timeout==2.4.0
pytest-cov==7.1.0
hypothesis==6.165.3
tomli==2.2.1; python_version < "3.11"

# Documentation and release profiles.
mkdocs==1.6.1
mkdocs-material==9.6.21
build==1.3.0
twine==7.0.0
hatchling==1.32.0
